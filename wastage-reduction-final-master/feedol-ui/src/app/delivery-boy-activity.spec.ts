import { DeliveryBoyActivity } from './delivery-boy-activity';

describe('DeliveryBoyActivity', () => {
  it('should create an instance', () => {
    expect(new DeliveryBoyActivity()).toBeTruthy();
  });
});
