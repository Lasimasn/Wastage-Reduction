import { RestaurantActivity } from './restaurant-activity';

describe('RestaurantActivity', () => {
  it('should create an instance', () => {
    expect(new RestaurantActivity()).toBeTruthy();
  });
});
